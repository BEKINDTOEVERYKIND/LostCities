Candidate set = the policy's above-floor top moves, nothing else
2026-09-04, declared before any game.

Reviewer directive (2026-09-04): the search must not spend playouts on
pile-draw variants of the top play that the policy did not rank; only
the top policy plays are evaluated.  Two mechanisms were still doing
it in the review games (fs12, 3 games, 137 searched plies):
  1. the ANALYSIS (advisory display) string kept draw_filter=0, so the
     two top-prior actions were expanded over every legal pile draw:
     draw variants of the top action on 84 of 137 advisory lists.
  2. eval_cand=4 in every string: when the 2% floor leaves fewer than
     four candidates, the next policy moves BELOW the floor are added as
     advisory candidates (a full dets x playout sweep each; reachable
     only through the 3-SE/4-point override).  At play time those are
     mostly draw variants of the top play at 1-2% prior (e.g. ply 34 of
     game 909200: "W9 play, draw R" at 0.015 evaluated beside three
     above-floor candidates).
Change: eval_cand 4 -> 0 in all strings (field 8) and draw_filter 2 in
the analysis string (field 22).  The evaluated set is then exactly the
policy's top root_width (5) moves at or above the 2% floor.  Draw
variants can still appear when the policy itself ranks them there
(they are policy plays by definition).  The override fields (ovk 3,
ovmin 4) stay in the string; with no advisory candidates they never
fire.
Strings:
  TOURNAMENT: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:0:0:1:3:4:0:0:0:1:0:0:0:0:0:2:0:120:1:120
  REVIEW:     rollouth:data/best.bin:data/belief_best.bin:512:5:0.02:0:1:14:0:0:0:1:3:4:0:0:0:1:0:0:0:0:0:2:0:120:1:120
  ANALYSIS:   rollouth:data/best.bin:data/belief_best.bin:512:5:0.02:0:1:0:0:0:0:1:3:4:0:0:0:0:0:0:0:0:0:2:0:120:1:120
Gate O (strength read of the directive, 800 games, seeds 5500-5579):
  A: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:0:0:1:3:4:0:0:0:1:0:0:0:0:0:2:0:120:1:120
  B: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:4:0:1:3:4:0:0:0:1:0:0:0:0:0:2:0:120:1:120
Rule: the directive stands regardless (reviewer's call); the gate is
reported with its CI and the wall-clock saving.  A result below 49.5%
is reported as the cost of the directive, not reverted silently.
Independent code audit (2026-09-04, three lenses, every finding
adversarially verified): under all three strings the evaluated set is
exactly the top root_width prefix at or above the floor after wager
dedup and dominated-discard pruning; the draw-variant expansion block
(rollout.c, guard eval_cand > 0 && draw_filter < 2) is the only site
that ever wrote past the sorted prefix and is dead; the eval_cand
widening only re-admits floor-cut moves inside the prefix and is dead
at 0; the override loop has an empty range; "search"/"playsearch" in
the analysis dump are exactly the evaluated candidates.  No code
change needed beyond the strings.
Gate O result (2026-09-04 17:10 UTC, 800 games, seeds 5500-5579):
  W/L/D 412/386/2  score 51.62% +/- 3.46 (95% CI); paired margin +2.53 +/- 1.43 pts/game; points/game 117.85 vs 115.32; chunks 80
  The directive costs nothing measurable and leans positive (margin
  +1.5 SE); the eval_cand 0 string stands.  Wall-clock: see chunk time
  vs Gate K below in the README line.

Action-level candidates (draw_filter 3) -- pre-registered 2026-09-04,
declared before any game.
After the eval_cand change, draw variants of the top play still appear
in 69 of 340 play-time candidate lists (fs13) because the policy head
factors P(move) = P(action) P(draw): a confident action (0.79) times a
5% draw source outranks every other play, so the joint ranking fills
the five slots with the top action's alternative draws.  draw_filter 3
groups moves by (card, disposition), ranks by the group's summed prior
and searches each action with its most probable draw source: the
search compares plays, the draw source is the policy's.  One-round
check (seed 910001, 96 worlds): lists with repeated actions 0.
Gate P (800 games, seeds 5600-5679, after Gate O):
  A: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:0:0:1:3:4:0:0:0:1:0:0:0:0:0:3:0:120:1:120
  B: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:0:0:1:3:4:0:0:0:1:0:0:0:0:0:2:0:120:1:120
Rule: >= 52% adopt and report the string; otherwise report the result
and the reviewer decides between "policy-ranked draw variants are
plays" (standing) and "plays are actions" (draw_filter 3).
Gate P result: pending
