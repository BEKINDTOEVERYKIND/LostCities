Selection confirmation (sel_confirm, spec field 24) -- pre-registration
2026-08-29, before any gate game runs.

Trigger: reviewer caught ply 16 of showcase game 909200 (c20 era):
policy 76% on play G8, deep analysis Q best for play G8 (-1.2), yet the
game played disc W2 draw-B (Q -3.1, the worst of the top four).  Replay
of the exact state x40 fresh seeds at the match spec: G8 chosen 34/40,
the played move 2/40 -- a ~5% noise tail of the sel_k selection gate
(one 96-world batch, 1-SE bar, four alternatives), not a code bug.
The reviewer's verdict (G8 strictly superior) matches the search's own
majority and the deeper analysis.

Mechanism: a sel_k qualifier must additionally lead the policy top on a
FRESH batch of dets sampled worlds (argmax playouts, paired, margin > 0)
or the top plays.  Asymmetric by design: real leads survive any
independent retest (the +2.61 +- 0.24 safe-wager discard that motivated
sel_k), one-SE flukes fail a fresh batch about half the time.  This is
what a stiffer sel_k could not do -- k=1.5 suppressed good overrides
and measured worse (README).  Cost: one extra world batch only when a
qualifier fires.

On the caught state (40 seeds): G8 30/40 -> 36/40; W2 discards 4 -> 2.

Gate G -- 800 games, seeds 4400-4479
  A: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:4:0:1:3:4:0:0:0:1:0:0:0:0:0:2:1
  B: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:4:0:1:3:4:0:0:0:1:0:0:0:0:0:2
  Declared verdicts (my initiative -> the standard spec-change bar):
    >=52%      adopt A as recommended match spec, report exact string
    49.5-52%   measured-neutral: keep standing spec, but report to the
               reviewer with the suite/probe evidence and a
               recommendation -- removing reviewer-visible 5%-tail
               blunders at zero strength cost may justify adoption on
               their call even inside the noise band
    <49.5%     negative, record, keep standing spec

RESULTS: pending
