Selection confirmation (sel_confirm, spec field 24) -- pre-registration
2026-08-29, before any gate game runs.

Trigger: reviewer caught ply 16 of showcase game 909200 (c20 era):
policy 76% on play G8, deep analysis Q best for play G8 (-1.2), yet the
game played disc W2 draw-B (Q -3.1, the worst of the top four).  Replay
of the exact state x40 fresh seeds at the match spec: G8 chosen 34/40,
the played move 2/40 -- a ~5% noise tail of the sel_k selection gate
(one 96-world batch, 1-SE bar, four alternatives), not a code bug.
The reviewer's verdict (G8 strictly superior) matches the search's own
majority and the deeper analysis.

Mechanism: a sel_k qualifier must additionally lead the policy top on a
FRESH batch of dets sampled worlds (argmax playouts, paired, margin > 0)
or the top plays.  Asymmetric by design: real leads survive any
independent retest (the +2.61 +- 0.24 safe-wager discard that motivated
sel_k), one-SE flukes fail a fresh batch about half the time.  This is
what a stiffer sel_k could not do -- k=1.5 suppressed good overrides
and measured worse (README).  Cost: one extra world batch only when a
qualifier fires.

On the caught state (40 seeds): G8 30/40 -> 36/40; W2 discards 4 -> 2.

Gate G -- 800 games, seeds 4400-4479
  A: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:4:0:1:3:4:0:0:0:1:0:0:0:0:0:2:1
  B: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:4:0:1:3:4:0:0:0:1:0:0:0:0:0:2
  Declared verdicts (my initiative -> the standard spec-change bar):
    >=52%      adopt A as recommended match spec, report exact string
    49.5-52%   measured-neutral: keep standing spec, but report to the
               reviewer with the suite/probe evidence and a
               recommendation -- removing reviewer-visible 5%-tail
               blunders at zero strength cost may justify adoption on
               their call even inside the noise band
    <49.5%     negative, record, keep standing spec

OUTCOME, round 1 (fresh-batch veto): REFUTED at the probe level before
any gate game -- the adversarial probe sweep (38 probes x 20 seeds)
found 6 improvements but 12 regressions carrying the exact k=1.5
signature: reviewer-verified good overrides (fs1_p70's Y8, selk_p23's
W2, fs1_p29's R8-take-G, fs1_p71's W6, fs1_p15's Yx, g424_p118's G8+Y)
reverted toward the worse policy top.  The asymmetry argument fails
because most good overrides are SMALL true leads (+0.34 +- 0.26 scale),
which fail an independent margin>0 retest nearly as often as flukes.
Mechanism withdrawn without a gate.  The playsearch/viewer honesty fix
stands.

Round 2 (sel_deep, field 24 redefined): pooling instead of veto.  When
any eligible candidate outscores the policy top on the first batch, a
second batch of dets worlds is bought and ALL selection/override
decisions use the pooled 2x statistics.  Adding data shrinks the SE:
real small leads qualify more often, flukes less -- the ROC moves
instead of sliding.  Pre-registered pipeline, declared before any
measurement beyond 5 spot probes (which were 20-seed inconclusive):
  1. Full suite, ON spec (standing + :1) vs the recorded OFF suite
     (341 better / 148 worse, suite_results_latest.txt, same harness
     and seeds).  Proceed to Gate G only if ON is not worse on the
     aggregate (better >= 341 - 8 and worse <= 148 + 8 tolerance =
     one probe's worth); otherwise record refuted and stop.
  2. Gate G, 800 games, seeds 4400-4479:
     A: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:4:0:1:3:4:0:0:0:1:0:0:0:0:0:2:1
     B: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:4:0:1:3:4:0:0:0:1:0:0:0:0:0:2
     >=52% adopt A (report exact string); 49.5-52% keep standing spec
     and report to reviewer with evidence + recommendation; <49.5%
     negative, record.

RESULTS:
Round 2 suite (sel_deep, standing spec + :1, 20 seeds, same harness):
  342 better / 160 worse  vs OFF baseline 341 / 148.
  Pre-registered precondition was better >= 333 AND worse <= 156:
  FAILED on the worse count (+12).  Pooling helped nothing on net --
  the extra batch re-litigates decisions the single batch already got
  right about as often as it fixes the ones it got wrong, at 1.3-1.5x
  contested-ply cost.  REFUTED; no Gate G games were played.

Standing verdict for the ply-16 noise class: the sel_k=1 single-batch
gate remains the measured optimum among {ungated, k=1, k=1.5,
fresh-batch veto, pooled deepening}.  Its ~5%-tail selection noise on
states like the reviewer's catch is, on present evidence, the price of
keeping the small-true-lead overrides that the suite and the k=1
match gate reward.  Fields 24's mode stays in the engine as an
experimental knob, NOT in the recommended spec.  What ships from this
arc: the play-time search is now recorded (analyze playsearch) and
displayed in the viewer, so every future review audits the search
that actually chose the move.
