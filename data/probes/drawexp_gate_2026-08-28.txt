Draw-variant expansion removal -- pre-registration, 2026-08-28
Reviewer directive: search must stop always evaluating every draw source of
the top play; evaluate only the top policy plays.  Implemented as
draw_filter=2 (spec field 23): the advisory draw-variant expansion is
suppressed entirely; the eval set is the policy-ranked candidates alone.

Measured on a fixed seed (4242, 3 rounds, identical trajectories):
  candidates evaluated per searched ply  6.52 -> 4.26
  wall-clock per game                    44.0s -> 29.8s  (1.47x)

Gate C -- removal at unchanged dets, 800 games, seeds 3600-3679
  A: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:4:0:1:3:4:0:0:0:1:0:0:0:0:0:2
  B: rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:4:0:1:3:4:0:0:0:1
  Declared before any game runs:
    >=49.5%  adopt A (user-directed change; the bar is only that it must
             not measurably hurt -- the win is the 1.47x compute saving)
    <49.5%   the removal measurably hurts: do NOT silently adopt; report
             the evidence to the reviewer (it would mean the full-bar
             override on draw variants was buying real wins).

Gate D -- reallocation: freed compute as more worlds, 800 games, seeds 3800-3879
  (runs only if Gate C adopts)
  A: Gate C spec with dets 96 -> 144 (~wall-clock parity with the OLD
     96-dets expanded search: 29.8 * 144/96 = 44.7s vs 44.0s)
  B: the Gate C spec (96 dets)
  Declared: >=52% adopt A (strength spec change bar); 49.5-52% neutral,
  keep the faster 96-dets Gate C spec; <49.5% negative, record.

Adversarial pre-gate review (3-agent workflow), key outcomes:
  - all 18 fs1_* reviewer probes: byte-identical 20-seed tallies OLD vs
    NEW (every move count, not just the top); NEW ~30% faster per
    searched ply.  On the ground-truth set the expansion was pure waste.
  - core correctness verified (indices, 22-field parse, no other
    draw_filter readers); fixes applied: negative draw_filter clamped,
    ov_draw incompatibility documented, README field-23 text updated.
  - suite-comparability caveat: manifest probes whose "better" move is a
    sub-floor draw variant of the policy-top action (g424_drawY_112,
    g424_G8Y_118, selk_stall_43/89/91/93) are unreachable under a =2
    spec by construction; suite aggregates under the new spec are not
    comparable with pre-field-23 runs.  Those probes remain meaningful
    for the labeler/analysis configs, which keep the expansion.
  - tools deliberately unaffected: mine.c labeler and train.c gen keep
    draw_filter=0 (they NEED variant pricing to generate corrections);
    analysis -a specs keep the expansion so the viewer still shows
    draw-variant Q rows; only the -p (play) spec carries =2.

RESULTS:
Gate C: W/L/D 401/394/5  score 50.44% +/- 3.46 (95% CI)
        points/game 117.39 vs 116.90, margin +0.49
  Verdict per pre-registration: >=49.5% -> ADOPTED.  The removal is
  strength-neutral at equal dets and 1.47x cheaper per game.  New
  recommended match spec:
  rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:4:0:1:3:4:0:0:0:1:0:0:0:0:0:2
Gate D: pending
