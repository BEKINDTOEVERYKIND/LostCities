Leaf-estimator gates W and X, declared 2026-09-05 02:40 UTC before running
==========================================================================
Where the round-3 evidence points: Bound U (perfect hand knowledge worth
+3.5 points), the closed resolution family (more worlds / deeper selection
neutral) and Gate V (a stronger endgame net in the leaves: 50.56%) all say
the tournament search is limited by what its estimator SCORES, not by what
it knows or how many worlds it averages.  Two estimator changes at the
leaves, both zero-cost, both pre-tested single-threaded by the round-3
panel on stored decisions:

Gate W -- per-world relabeled playouts (sym_play, spec field 30).
  The playout policy is the one unsymmetrized component: candidates are
  formed from the exact 120-relabeling average (sym_k), worlds from the
  symmetrized belief (sym_bel), but every playout runs the raw net in the
  identity suit frame, so its suit habits enter every world's margin as a
  bias that is constant across worlds and invisible to the paired SE.
  With sym_play 1 each world is played out under its own random relabeling
  drawn from the world's seed (the world stream is unchanged; every
  candidate shares the world and the frame; the round margin is
  frame-invariant), so the estimator is exactly suit-invariant in
  expectation.  Panel pre-test (117 stored round-2 decisions, 96 fixed
  worlds): the pick differs on 31% of decisions; pooling two frames cuts
  the paired variance by 17% (about 64% of the paired variance is frame
  noise), override rate 0.35 -> 0.27; on the 41 reviewer probes x 3 seeds
  better/worse 44/27 -> 55/23.  Panel 1 had dropped the idea at critique
  ("MSE unchanged at fixed worlds") without a gate; the symmetry rule
  ("everything should be symmetrized") applies to it.
  A = rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:0:0:1:3:4:0:0:0:1:0:0:0:0:0:2:0:120:1:120:0:0:1
  B = rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:0:0:1:3:4:0:0:0:1:0:0:0:0:0:2:0:120:1:120
  seeds 6400-6449 (50 chunks x 8 pairs = 800 games), runner $SP/build3/bin/arena
  (source of commit <this>), -n 8 -r 3 -t 4.
  >= 52%  adopt on strength; 49.5-52% adopt under the symmetry rule (as
  Gate K's exact-vs-sampled read was), stated as such; < 49.5% reject.
  Suite tripwire 20 seeds (consistent if within 13 of standing).

Gate X -- smooth match objective in every round (win_q 2).
  win_q 1 (the measured "no better than margin" result, 50.4% pooled over
  2,000 pairs) never acted: it only re-orders the FINAL-round pick among
  candidates that already beat the policy top on margin by 1 SE, and the
  panel's replay of 299 stored round-2 decisions found it differs from
  the deployed rule on 1.0% of them.  win_q 2 scores every world as
  100*Phi((cum_diff + m) / s_left), s_left^2 = rounds_after*39^2 + 8^2
  (39 = one-round margin SD between tournament agents, panel-1
  calibration; 8 = the final-round softness at the estimator's own
  resolution), and selection, the sel_k gate and the override run on
  that statistic unchanged; rounds 1-2 reduce to points except in
  blowouts, the final round becomes a soft win step.  Panel replay: the
  consistent objective flips 16-24% of final-round decisions and ~10% of
  round-1/2 decisions -- a real estimand change, unlike win_q 1.
  Reported q stays in points.
  A = rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:0:2:1:3:4:0:0:0:1:0:0:0:0:0:2:0:120:1:120
  B = standing (as above).  seeds 6450-6499, same runner.
  >= 52% adopt (report the string); 49.5-52% neutral (recorded; the
  constants 39/8 get ONE variant at most, pre-registered separately);
  < 49.5% negative.  Suite tripwire as above.
Both gates run after V-ctl (seeds 6350-6399) finishes; identity of the
standing side under the new binary is checked by replaying Bound U chunk
6200 (must reproduce W/L/D 6/10/0, margin +5.94).
Gate W result (2026-09-05 03:38 UTC, 37.7 s/chunk, identity check passed):
  W/L/D 394/400/6  score 49.62% +/- 3.46 (95% CI); paired margin -1.65 +/- 1.51 pts/game; points/game 112.97 vs 114.62; chunks 50
  chunk-cluster SE 1.41%.  Wins at parity (49.62%, inside the declared
  49.5-52 band by 0.12), points 1.65 +/- 1.51 lower (z = -1.1, not
  significant).  Strength reading: no measurable change either way -- the
  panel's mechanism critic had measured the removable frame bias at 3-5x
  below the proposal's claim, and this is what "3-5x below" looks like at
  800 games.  Disposition by the declared rule: the symmetry rule applies
  (the playout policy was the last unsymmetrized component), so sym_play 1
  is adopted AS A SYMMETRY CHANGE, not a strength claim, subject to the
  suite tripwire (20 seeds, within 13 of standing) and to the owner's
  veto given the bottom-of-band read; the final string is composed after
  Gate X (a joint confirmation is run if both adopt).
  Suite tripwire (20 seeds, 42 probes, same binary): standing 323 better /
  170 worse; sym_play 319 / 149 -- within 13 on the better column, 21
  fewer worse.  Consistent; tripwire passed.
Gate X result (2026-09-05 04:07 UTC, 38.5 s/chunk):
  W/L/D 395/402/3  score 49.56% +/- 3.46 (95% CI); paired margin -1.90 +/- 1.59 pts/game; points/game 112.01 vs 113.91; chunks 50
  chunk-cluster SE 1.36%.  NEUTRAL: wins at parity, points 1.9 +/- 1.6
  lower.  The objective that flips 16-24% of final-round picks toward the
  win-probability argmax wins no more matches than the points argmax at
  the same worlds -- the same lesson as win_q 1's tiebreak read and the
  candidate floor: at fixed compute the statistically efficient objective
  is not beaten by the theoretically right one, because the flips it
  buys are among candidates whose win difference is inside the
  estimator's noise.  Not adopted; the single pre-registered variant
  (FINAL_SD 3, a sharper final-round step) is NOT run now -- a sharper
  step buys more of the same flips.  win_q 2 stays a documented mode.
Adopted string after Gates W and X (sym_play 1, win_q 0):
  rollouth:data/best.bin:data/belief_best.bin:96:5:0.02:0:1:14:0:0:0:1:3:4:0:0:0:1:0:0:0:0:0:2:0:120:1:120:0:0:1
  review = same with 512 worlds.
